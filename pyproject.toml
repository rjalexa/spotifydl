[project]
name = "spotifydl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "spotipy>=2.25.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.10",
]
